#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
ARTIFACTS_DIR="$ROOT_DIR/artifacts/jit-native-ws-approval-outcomes"
rm -rf "$ARTIFACTS_DIR"
mkdir -p "$ARTIFACTS_DIR"

APPROVAL_OUTCOMES_ARTIFACTS_DIR="$ARTIFACTS_DIR" \
APPROVAL_OUTCOMES_RUNTIME_MODE="jit" \
APPROVAL_OUTCOMES_ORCHESTRATOR="native" \
APPROVAL_OUTCOMES_GATEWAY_PORT="${APPROVAL_OUTCOMES_GATEWAY_PORT:-22131}" \
APPROVAL_OUTCOMES_PROVIDER_PORT="${APPROVAL_OUTCOMES_PROVIDER_PORT:-22132}" \
APPROVAL_OUTCOMES_PUBLISH_AOT=0 \
APPROVAL_OUTCOMES_ENABLE_MAF=0 \
bash "$ROOT_DIR/eng/verify-websocket-approval-outcomes-smoke.sh"
